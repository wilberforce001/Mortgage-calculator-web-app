# Initiliaze Node.js Project
- npm init -y
- install required packages; npm install express pg dotenv
- npm install pg -  to configure the backend and connect with the database by installing pg library.
